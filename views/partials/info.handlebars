<div class="card mb-5">
    <div class="card-header bg-secondary">
        <h4 class="card-title"><a class="text-info" href="/post/{{id}}">{{title}}</a></h4>
        <h6 class="card-subtitle mb-2 text-light">This post was by{{user.username}} on {{made_in}}</h6>
    </div>
    <div class="card-body">
        <p class="card-text">{{fufilled}}</p>
    </div>
</div>